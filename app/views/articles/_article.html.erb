<div id="<%= dom_id article %>">
  <p>
    <strong>Title:</strong>
    <%= article.title %>
  </p>

  <%= cl_image_tag article.photo.key, height: "200px" if article.photo.attached?%>

  <p>
    <strong>Body:</strong>
    <%= article.body %>
  </p>

</div>
